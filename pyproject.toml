[project]
name = "sap-cloud-sdk"
version = "0.40.0"
description = "SAP Cloud SDK for Python"
readme = "README.md"
license = "Apache-2.0"
authors = [{ name = "SAP SE", email = "ospo@sap.com" }]
requires-python = ">=3.11"
dependencies = [
    "minio~=7.2.16",
    "setuptools~=80.9.0",
    "requests>=2.33.0",
    "requests-oauthlib~=2.0.0",
    "pydantic~=2.12.3",
    "hatchling~=1.27.0",
    "opentelemetry-exporter-otlp-proto-grpc~=1.42.1",
    "opentelemetry-exporter-otlp-proto-http~=1.42.1",
    "traceloop-sdk~=0.61.0",
    "opentelemetry-instrumentation-langchain>=0.61.0",
    "httpx>=0.27.0",
    "PyJWT>=2.13.0",
    "protobuf>=4.25.0",
    "protovalidate>=0.13.0",
    "grpcio>=1.60.0",
    "opentelemetry-api>=1.42.1",
    "opentelemetry-sdk>=1.42.1",
    "opentelemetry-instrumentation-httpx~=0.63b1",
    "opentelemetry-instrumentation-requests~=0.63b1",
    "opentelemetry-instrumentation-grpc~=0.63b1",
    "opentelemetry-instrumentation-logging~=0.63b1",
    "opentelemetry-instrumentation-starlette~=0.63b1",
    "opentelemetry-instrumentation-fastapi~=0.63b1",
    "opentelemetry-instrumentation-aiohttp-client~=0.63b1",
    "opentelemetry-instrumentation-sqlalchemy~=0.63b1",
    "opentelemetry-instrumentation-django~=0.63b1",
    "opentelemetry-instrumentation-flask~=0.63b1",
    "mcp>=1.1.0",
    "cryptography>=46.0.3",
]

[project.optional-dependencies]
extensibility = ["a2a-sdk>=0.2.0"]
starlette = ["starlette>=0.40.0"]
fastapi = ["fastapi>=0.100.0"]
aiohttp = ["aiohttp>=3.9.0"]
sqlalchemy = ["sqlalchemy>=2.0.0"]
django = ["django>=4.0"]
flask = ["flask>=3.0"]
langgraph = ["langgraph>=1.0.0"]
langgraph-checkpoint-sap-agent-memory = [
    "langgraph-checkpoint-sap-agent-memory @ git+https://github.tools.sap/AgentMemory/langgraph-checkpoint-sap-agent-memory.git@ttl-checkpoint",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.metadata]
allow-direct-references = true

[tool.hatch.build.targets.wheel]
packages = ["src/sap_cloud_sdk", "src/buf"]

[dependency-groups]
dev = [
    "pytest>=8.4.2",
    "pytest-cov>=7.0.0",
    "pytest-asyncio>=1.0.0",
    "pytest-bdd>=7.2.0",
    "python-dotenv>=1.0.0",
    "ty==0.0.64",
    "cryptography>=46.0.3",
    "ruff==0.16.0",
    "starlette>=0.40.0",
    "anyio>=3.6.2",
    "httpx>=0.27.0",
    "fastapi>=0.100.0",
    "aiohttp>=3.9.0",
    "sqlalchemy>=2.0.0",
    "django>=4.0",
    "flask>=3.0",
    "a2a-sdk>=0.2.0",
    "langchain-core>=1.2.7",
    "langgraph>=0.2.0",
    "langchain-community>=0.3.0",
    "litellm>=1.40.0",
    "langchain-litellm>=0.6.6",
    "langgraph-checkpoint-sap-agent-memory @ git+https://github.tools.sap/AgentMemory/langgraph-checkpoint-sap-agent-memory.git@ttl-checkpoint",
]

[tool.pytest.ini_options]
markers = [
    "integration: marks tests as integration tests (requires Docker)",
    "aicore: marks tests that require AI Core credentials (AICORE_* env vars)",
    "no_autouse_credentials: opts out of the default no-credentials fixture in checkpointer tests",
]

[tool.coverage.run]
source = ["src"]
omit = [
    "*/tests/*",
    "*/test_*",
    "*/sap_cloud_sdk/core/auditlog_ng/gen/*",
    "*/buf/*",
]
skip_empty = true

[tool.ruff]
exclude = [
    ".github",
    "__pycache__",
    ".ruff_cache",
    ".venv",
    ".vscode",
    "__pypackages__",
    "build",
    "dist",
    "venv",
    "tests/",
    "src/sap_cloud_sdk/core/auditlog_ng/gen",
    "src/buf",
]

# Same as Black.
line-length = 88
indent-width = 4

# Assume Python 3.11+
target-version = "py311"

[tool.ruff.lint]
select = ["E9", "F", "W"]

ignore = []

[tool.ruff.format]
# Same as black
quote-style = "double"
indent-style = "space"
skip-magic-trailing-comma = false
line-ending = "auto"

[tool.ty]

[tool.ty.environment]
python-version = "3.11"

[tool.ty.src]
include = ["src", "tests"]
exclude = ["src/sap_cloud_sdk/core/auditlog_ng/gen", "src/buf"]
